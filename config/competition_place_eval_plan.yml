# Local red-team run (place)
# Output: state recording (.npz) only, no video.

stat_saving_enabled: false
state_recording_enabled: true
video_capture_enabled: false

evaluation_plan:
  - map_name: Map01
    team_mode: place
    nb_rounds: 2
    config_weight: 1
    number_drones: 5
    initial_bombs_per_drone: 2
    zones_config: []
